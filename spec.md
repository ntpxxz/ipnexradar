```python?code_reference&code_event_index=5
# Using a different approach to write the string to avoid the triple-quoted string error

md_content_lines = [
    "# Software Specification: IP Monitoring & Anti-Spoofing System",
    "",
    "## 1. Project Overview",
    "ระบบสำหรับตรวจสอบและติดตามอุปกรณ์ภายในเครือข่าย โดยเน้นการระบุตัวตนด้วยการผูกเอกลักษณ์ (Identity Binding) ระหว่าง IP, MAC Address และ Hostname เพื่อตรวจจับการเปลี่ยนแปลงและป้องกันการสวมรอย (Spoofing)",
    "",
    "---",
    "",
    "## 2. Detailed System Workflow",
    "กระบวนการทำงานถูกแบ่งออกเป็น 4 ระยะหลัก เพื่อความแม่นยำในการระบุตัวตน:",
    "",
    "### 2.1 Discovery Phase (การสำรวจ)",
    "- **Scanner Service:** ทำการส่ง ARP Request (สำหรับ Local Subnet) หรือ ICMP/SNMP (สำหรับต่างวง) เพื่อดึงข้อมูลอุปกรณ์",
    "- **Data Collection:** รวบรวมชุดข้อมูล `[IP, MAC Address, Hostname, Active Port]`",
    "",
    "### 2.2 Analysis & Comparison Phase (การวิเคราะห์)",
    "ระบบจะนำข้อมูลที่สแกนได้ไปเปรียบเทียบกับฐานข้อมูลล่าสุด (Latest State) โดยใช้ Logic ดังนี้:",
    "1.  **Match MAC:** หากพบ MAC Address เดิมในฐานข้อมูล",
    "    - ถ้า IP เปลี่ยน -> ติด Tag `[IP_MOVED]`",
    "    - ถ้า Hostname เปลี่ยน -> ติด Tag `[HOSTNAME_CHANGED]`",
    "2.  **New MAC:** หากไม่พบ MAC Address ในฐานข้อมูล -> ติด Tag `[NEW_DEVICE]`",
    "3.  **Conflict Detection (Anti-Spoofing):**",
    "    - หากสแกนพบ IP ที่เคยมีอยู่แล้ว แต่ **MAC Address ไม่ตรงกับที่บันทึกไว้** -> ติด Tag `[⚠️ SPOOFING_ATTEMPT]` และส่ง Alert ทันที",
    "",
    "### 2.3 Persistence Phase (การบันทึก)",
    "- บันทึกผลการสแกนลงใน `scan_logs`",
    "- อัปเดตสถานะปัจจุบันในตาราง `devices` เพื่อใช้เป็นฐานข้อมูลอ้างอิงในการสแกนรอบถัดไป",
    "",
    "### 2.4 Notification & Reporting Phase (การแจ้งเตือน)",
    "- ส่ง Alert ผ่าน Line Notify / Email หากพบสถานะ Critical",
    "- ประมวลผลรายงานสรุปประจำรอบ (Shift) ในรูปแบบ Excel",
    "",
    "---",
    "",
    "## 3. Database Schema (PostgreSQL)",
    "",
    "```sql",
    "-- 1. ตารางเก็บข้อมูลอุปกรณ์หลัก (Master Data)",
    "CREATE TABLE devices (",
    "    id SERIAL PRIMARY KEY,",
    "    mac_address VARCHAR(17) UNIQUE NOT NULL, -- Key หลักในการระบุตัวตน",
    "    last_known_ip VARCHAR(15),",
    "    last_known_hostname VARCHAR(255),",
    "    category VARCHAR(50) DEFAULT 'Unknown', -- PC, Server, Printer, Mobile",
    "    is_authorized BOOLEAN DEFAULT FALSE,    -- สำหรับแยกเครื่องที่อนุญาตกับเครื่องแปลกปลอม",
    "    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,",
    "    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP",
    ");",
    "",
    "-- 2. ตารางเก็บประวัติการสแกน (Audit Trail)",
    "CREATE TABLE scan_logs (",
    "    id SERIAL PRIMARY KEY,",
    "    device_id INTEGER REFERENCES devices(id),",
    "    ip_address VARCHAR(15) NOT NULL,",
    "    hostname VARCHAR(255),",
    "    status_tag VARCHAR(50), -- NEW, NORMAL, IP_MOVED, SPOOFING",
    "    scan_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,",
    "    is_alert_sent BOOLEAN DEFAULT FALSE",
    ");",
    "",
    "-- 3. ตารางตั้งค่าระบบ",
    "CREATE TABLE system_configs (",
    "    key VARCHAR(50) PRIMARY KEY,",
    "    value TEXT,",
    "    description TEXT",
    ");",
    "```",
    "",
    "---",
    "",
    "## 4. Technical Stack & Security",
    "",
    "### Tech Stack",
    "- **Backend:** Python 3.11+ (FastAPI)",
    "- **Scanning Library:** Scapy (ARP/Layer 2), Nmap (Service/Port Discovery)",
    "- **Database:** PostgreSQL",
    "- **Frontend:** Next.js + Tailwind CSS",
    "- **Task Runner:** Celery + Redis (สำหรับจัดการคิวการสแกนอัตโนมัติ)",
    "",
    "### Security Features",
    "- **IP-MAC Binding:** ตรวจสอบความถูกต้องของคู่ IP/MAC ทุกครั้งที่มีการสแกน",
    "- **JWT Authentication:** สำหรับการเข้าใช้งาน Dashboard จากภายนอก",
    "- **Rate Limiting:** ป้องกันการสแกนที่ถี่เกินไปจนทำให้ Network ทำงานหนัก (Network Congestion)",
    "- **Encrypted Logs:** ข้อมูลประวัติการสแกนจะถูกป้องกันการแก้ไข (Read-only for regular users)"
]

with open("system_spec.md", "w", encoding="utf-8") as f:
    f.write("\n".join(md_content_lines))